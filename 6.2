def sumDigits(n):
    sum = 0
    while n > 0:
        digit = n % 10
        sum += digit
        n = n // 10
    return sum

num = int(input("Enter an integer: "))
print("The sum of the digits in {} is {}".format(num, sumDigits(num)))
