#3.1/page85
# Write a program that prompts the user to enter the length from the center of a pentagon to a vertex and computes the area of the pentagon

import math
length = float(input("Enter the length from the center to a vertex: "))
side_length = 2 * length * math.sin(math.pi/5)
area = ((3 * math.sqrt(3)) / (2))*(side_length**2)
print(f"The area of the pentagon is: {area:.2f}")
